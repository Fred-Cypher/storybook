{% extends 'base.html.twig' %}

{% block title %}Administration des jeux vidéos{% endblock %}

{% block body %}
    <main class="container">
        <section class="row mt-5">
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <th>Id</th>
                        <th>Titre</th>
                        <th>Jaquette</th>
                        <th>Actions</th>
                    </thead>
                    <tbody> 
                        {% for recentGame in recentGames %}
                            <tr>
                                <td>{{ recentGame.id }}</td>
                                <td>{{ recentGame.title }}</td>
                                <td><img src="{{ asset('assets/uploads/illustrations/miniatures/300x300-' ~ recentGame.illustrations[0].name) }}" class="mini" alt="Illustration du jeu"></td>
                                <td class="text-wrap">
                                    <a href="{{ path('app_games_edit_recent_game', {'id': recentGame.id}) }}" class="btn btn-outline-warning">Modifier</a>
                                    <a href="{{ path('app_games_delete_recent_game', {'id': recentGame.id}) }}" class="btn btn-outline-danger">{{ include('admin/recentGames/_delete_form.html.twig') }}</a>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </section>
    </main>
{% endblock %}